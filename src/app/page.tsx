export default function Home() {
  return (
    <main className="container flex min-h-screen items-center py-12">
      <section className="max-w-2xl">
        <p className="mb-5 text-sm uppercase tracking-[0.16em] text-muted">
          Новый сайт
        </p>
        <h1 className="font-semibold">Основа готова.</h1>
        <p className="mt-8 max-w-xl text-muted">
          Здесь постепенно появится интерфейс — секция за секцией.
        </p>
      </section>
    </main>
  );
}
